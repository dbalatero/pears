<p style="color: green"><%= notice %></p>

<h1>Pears</h1>

<div id="pears">
  <% @pears.each do |pear| %>
    <img width="300" src="<%= pear.url %>" />

    <p>
      <%= link_to "Show this pear", pear %>
    </p>
  <% end %>
</div>

<%= link_to "New pear", new_pear_path %>
